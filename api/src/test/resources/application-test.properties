spring.datasource.url=jdbc:postgresql://localhost:5432/runway_test
spring.datasource.username=${DB_USERNAME:runway}
spring.datasource.password=${DB_PASSWORD:runway}

spring.jpa.hibernate.ddl-auto=validate

runway.jwt.secret=test-only-secret-not-for-production-use-min-32-bytes
runway.scheduler.enabled=false
